# MinIO/S3 Configuration for Script Asset Mirroring
# This service mirrors Blood on the Clocktower script images to MinIO storage

# S3-compatible MinIO endpoint (internal Docker network address)
S3_ENDPOINT=http://minio:9000

# MinIO credentials (use MINIO_ROOT_USER and MINIO_ROOT_PASSWORD from main .env)
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin123

# S3 region (required but not used by MinIO)
S3_REGION=us-east-1

# Bucket name - using the same bucket as user character images
S3_BUCKET=user-images

# Path prefix within the bucket to organize script assets separately
# Production: script-assets
# Beta: beta/script-assets
S3_PATH_PREFIX=script-assets

# Public URL base for accessing mirrored images
# Production: https://user-images.clocklaboratory.com/script-assets
# Beta: https://beta-images.clocklaboratory.com/beta/script-assets
S3_PUBLIC_BASE_URL=https://user-images.clocklaboratory.com/script-assets

# Force path-style S3 URLs (required for MinIO)
S3_FORCE_PATH_STYLE=true

# Optional: Use US proxy for downloading script images (usually not needed)
USE_US_PROXY=false
# US_PROXY_LIST_URL=https://example.com/proxy-list.txt

# Optional: Show proxy option in UI (defaults to false if not set)
# Set to true to show the proxy checkbox to users
# ENABLE_PROXY_OPTION=true

# Node environment
NODE_ENV=production
